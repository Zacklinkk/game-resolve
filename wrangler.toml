# Cloudflare Pages 配置
name = "game-resolve"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[env.production]
routes = [
  { pattern = "/*", zone_name = "pages.dev" }
]

# 构建配置（用于 wrangler pages deploy）
[build]
command = "npm run build:github"
cwd = "."
watch_paths = []
